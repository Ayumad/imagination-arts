import Link from "next/link";
import type { Program } from "@/lib/site-data";

export function AgeGroupPage({ program }: { program: Program }) {
  return <><section className="page-hero"><p className="eyebrow">{program.age}</p><h1>{program.pageTitle}</h1><p className="page-hero__lede">{program.pageIntro}</p></section><section className="section"><div className="about-grid"><div><p className="eyebrow">What we explore</p><h2>{program.name}</h2><p className="body-large">{program.description}</p><div className="tag-list">{program.focuses.map((focus) => <span className="tag" key={focus}>{focus}</span>)}</div><Link className="button button--coral" href="/contact">Ask about this group</Link></div><div className={`age-art-panel age-art-panel--${program.tone}`} aria-hidden="true"><div className="age-art-panel__paper" /><div className="age-art-panel__shape age-art-panel__shape--one" /><div className="age-art-panel__shape age-art-panel__shape--two" /><div className="age-art-panel__dots" /></div></div></section><section className="section section--cream"><p className="eyebrow">This might be a great fit for</p><h2>{program.age} artists who are ready to...</h2><ul className="fit-list">{program.idealFor.map((item) => <li key={item}>{item}</li>)}</ul></section><section className="section callout"><p className="eyebrow">Let&apos;s find the right fit</p><h2>Ready to make something new?</h2><p>Share a little about your artist and we&apos;ll help with the next step.</p><Link className="button button--sun" href="/contact">Start a trial inquiry</Link></section></>;
}
